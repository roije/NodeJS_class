All orders are querying the database at the same time
Querying the database for an order takes five seconds
Instead of querying one order at a time, the database handles all orders at the same time.
So database finishes all requests in five seconds.

Difference between '==' and ''==='

19 == '19' will be 'true'.
'==' only compares the values.

19 === '19' will be 'false'
'===' compares values and types.
False because one is number and other is string.'

Prototype is adding special new functions or properties to objects.